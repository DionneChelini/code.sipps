version: 0.2

phases: 
  install:
    commands:
      # install Node 12
    curl -fsSL https://deb.nodesource.com/setup_12.x | -E bash - apt install -y nodejs

pre_build: 
  commands:
    #install dependencies
    - echo Installing dependencies
      - npm


build:
  commands:
    - echo Testing...
    - echo building...
    - npm run build

  artifacts:
    files
    - "**/*"
    discard-paths: no
    base-directory: dist